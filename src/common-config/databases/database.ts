export enum Databases {
  MongoDb = 'mongodb',
  Rdbms = 'rdbms',
}

export enum MongoDb {
  Uri = 'uri',
  UriTest = 'uriTest',
}

// TBC when I get to this point
export enum Rdbms {
  Uri = 'uri',
}
